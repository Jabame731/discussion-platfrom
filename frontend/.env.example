# Laravel API base URL (no trailing slash)
VITE_API_URL=http://localhost:8000/api/v1

# Typesense — use a *search-only* key here (never the admin key)
VITE_TYPESENSE_HOST=localhost
VITE_TYPESENSE_PORT=8108
VITE_TYPESENSE_PROTOCOL=http
VITE_TYPESENSE_SEARCH_KEY=your_typesense_search_only_api_key